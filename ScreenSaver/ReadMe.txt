# ScreenSaver

For installation, rename the compiled ScreenSaver.exe to ScreenSaver.scr, and then copy it to the System32 directory together with 1.wmv.

安装：编译，将ScreenSaver.exe重命名为ScreenSaver.scr，并连同1.wmv一同移动至System32目录。
